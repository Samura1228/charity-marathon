[build]
  # No build command needed — static site
  publish = "."

# Redirect /admin to admin.html for cleaner URLs
[[redirects]]
  from = "/admin"
  to = "/admin.html"
  status = 200

# SPA-style fallback is NOT needed (multi-page static site)
